{
  "name": "ronesis-project",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "panel"
  ],
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "panel": "npm run dev --prefix panel",
    "dev": "concurrently \"npm run client\" \"npm run server\" \"npm run panel\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}